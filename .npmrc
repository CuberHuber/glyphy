# Every published tarball carries a signed link back to the workflow run that
# built it. Requires the release job to hold `id-token: write`.
provenance=true

# A lockfile that drifts from package.json is a broken build waiting to happen.
save-exact=false
engine-strict=true
